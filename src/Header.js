const Header = () => {
  const headerStyle = {
    backgroundColor: "#000",
    color: "#fff",
  };
  return (
    <header style={headerStyle}>
      <p>
        <strong>H</strong>eader
      </p>
    </header>
  );
};

export default Header;
